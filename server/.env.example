# Backend environment variables
# Copy to .env (same folder) and fill in values.

PORT=4000
CLIENT_ORIGIN=http://localhost:5173

SUPABASE_URL=
SUPABASE_ANON_KEY=
# Service role key is REQUIRED on the server for admin operations.
SUPABASE_SERVICE_ROLE_KEY=
